#include<bits/stdc++.h>
using namespace std;
int main()
{
    // if  n is power of 2 por not
    // int n=16;
    // cout<<(n&&!(n&(n-1)));
    // cout<<" "<<(n&n-1);
    // (n&n-1) only diff here will be from right most set bit of n if we flip the bits it will turn to (n-1)
    //like (10110) ==(10101)  //and in case of perfect power of two 
    // n= 10000 n-1=01111 n&n-1==0
    // cout<<" "<<(!0);
    //to count bits in an efficient way
    // int n=19,i=0;
    // while(n!=0)
    //     n=n&(n-1),i++;
    // cout<<i;
    
    // int n=17;//10001
    //  cout<<(n^(1<<4));
  //to find rigth most set bit
//   cout<<(-10&10);

    
}
